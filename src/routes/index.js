const signInRoute = require('./login.route');
const userRoute = require('./user.route');
const categoryRoute = require('./category.route');

module.exports = {
  signInRoute,
  userRoute,
  categoryRoute,
};