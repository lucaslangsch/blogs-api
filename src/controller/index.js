const signInController = require('./login.controller');
const userController = require('./user.controller');
const categoryController = require('./category.controller');
const postController = require('./post.controller');

module.exports = {
  signInController,
  userController,
  categoryController,
  postController,
};